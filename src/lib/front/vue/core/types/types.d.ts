////////////////////////////////////////////////////////////
// GLOBAL TYPES !
////////////////////////////////////////////////////////////

// ATTENTION!!  If using import/export this will be treated as
//              a module, must use `declare global` & `export`

type Dictionary<T> = { [key: string]: T };
